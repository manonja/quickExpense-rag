"""
Database migrations (future-proofing).

Note: This project uses a "rebuild and replace" model for database updates.
Client-side migrations are not needed since databases are distributed as
versioned artifacts via GitHub Releases.

This file is reserved for potential future data transformation logic during
the build pipeline (e.g., converting data formats between schema versions).
"""

# Empty for schema v1.0
